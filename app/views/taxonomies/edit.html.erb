<h1>Editing</h1>

<%= form_for(@taxonomy) do |f| %>
  
	<%=render :partial => 'form',:locals => { :f => f }%>	
    <%= f.submit 'Update' %>


<% end %>

<%= button_to 'Show', @taxonomy ,{:method=>:get}%> 
<%= button_to 'Delete', @taxonomy,{:method=>:delete} %>