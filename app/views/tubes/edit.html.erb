<h1>Editing</h1>

<%= form_for(@tube) do |f| %>
  
	<%=render :partial => 'form',:locals => { :f => f }%>	
    <%= f.submit 'Update' %>


<% end %>

<%= button_to 'Show', @tube ,{:method=>:get}%> 
<%= button_to 'Delete', @tube,{:method=>:delete} %>